//If you want to access any element in an HTML page, 
//you always start with accessing the document object.

document.addEventListener('DOMContentLoaded',function(){
    //This line listens for the 'DOMContentLoaded' event, 
    //which is fired when the initial HTML document has been completely loaded and parsed. 
    //The anonymous function that follows will execute once the DOM is fully loaded.

    
})